# Requirements for DSS replication
# Based on original repository code analysis

# Core dependencies from original code
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0

# QWen2.5-VL specific
accelerate>=0.24.0
safetensors>=0.4.0
qwen-vl-utils

# Feature extraction and clustering
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
scikit-image>=0.22.0

# Leiden clustering (from utils.py and refine_leiden_utils.py)
scanpy>=1.9.0
anndata>=0.9.0
leidenalg>=0.9.0

# Image processing
opencv-python>=4.8.0
Pillow>=10.0.0
matplotlib>=3.7.0

# Progress tracking
tqdm>=4.65.0

# Additional utilities for spatial processing
pandas>=2.0.0

# SAM2 (if needed - check actual SAM2 package)
# segment-anything-2>=1.0.0

# Note: DINOv2 and SAM2 are loaded via transformers/huggingface
# QWen2.5-VL requires specific transformers version with VL support